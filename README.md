# Coding exercise using React (and TypeScript)
A simple react component that fetches users from an API and returns the list to the view.

Create-react-app functioned as the boilerplate for the application. 